<p id="notice"><%= notice %></p>

<!-- <h1>Contents</h1> -->

<div class="row">
  <% @contents.each do |content| %>
  <div class="col s3">
    <div class="card">
      <div class="card-image waves-effect waves-block waves-light">
        <%=  image_tag content.cover.url, class:"activator" %>
      </div>
      <div class="card-content">
        <span class="card-title activator grey-text text-darken-4"><%= content.titolo %><i class="material-icons right">more_vert</i></span>
        <div>
          <pan>Desiderato da <%= content.user.name if content.user %></pan>
          <pan class="right"><%= number_to_currency(content.price) %></pan>
        </div>        
        <br>
        <div class="center"><%= link_to 'Scpri di più', content,class: "btn" %></div>
        <% if content.user == current_user %>
          <br>
          <div class="center">
          <p><%= link_to 'Modifica', edit_content_path(content) %>
            <%= link_to 'Distruggi', content, method: :delete, data: { confirm: 'Sei sicuro?' } %></p>
          </div>
        <% end %>
        </div>
        <div class="card-reveal">
          <span class="card-title grey-text text-darken-4"><%= content.titolo %><i class="material-icons right">close</i></span>
          <p><%= content.descrizione %></p>
        </div>
      </div>
    </div>
    <% end %>
  </div>

<!--
<table>
  <thead>
    <tr>
      <th>Immagine</th>
      <th>Titolo</th>
      <th>Descrizione</th>
      <th>Utente</th>
      <th>Prezzo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contents.each do |content| %>
    <tr>
      <td><%=  image_tag content.cover.url(:thumb) %></td>
      <td><%= content.titolo %></td>
      <td><%= content.descrizione %></td>
      <td><%= content.user.name if content.user %></td>
      <td><%= content.price %></td>
      <td><%= link_to 'Mostra', content %></td>
      <% if content.user == current_user %>
      <td><%= link_to 'Modifica', edit_content_path(content) %></td>
      <td><%= link_to 'Distruggi', content, method: :delete, data: { confirm: 'Sei sicuro?' } %></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>
-->
<br>

<%= link_to 'New Content', new_content_path %>
